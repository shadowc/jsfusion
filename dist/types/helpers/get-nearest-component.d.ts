import { IComponent } from '../types/component';
import { IRuntime } from '../types/runtime';
export declare const getNearestComponent: (element: HTMLElement, componentName: string, JsFusion: IRuntime) => IComponent | null;
